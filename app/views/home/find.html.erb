<% @lecturers.each do |lecturer| %>
  <div id="info">
    <% if File::exists? RAILS_ROOT + "/public/images/#{lecturer.id}.png" %>
      <%= image_tag "/images/#{lecturer.id}.png" %>
    <% else %>
      <%= image_tag "/images/lecturer.png" %>
    <% end %>
      <%= lecturer.first_name %>
      <%= lecturer.last_name %>
  </div>
<% end %>
