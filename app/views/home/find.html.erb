<div id="login">
  <% form_tag :action => 'review' do %>
    <table class="custom">
      <tr>
        <td class="label"> Bölümler: </td>
        <td class="field"><%= select_tag(:id, options_from_collection_for_select(Departments.all, :id, :name)) %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= submit_tag "arama yap", :id => "submit_button" %></td>
      </tr>
    </table>
  <% end %>
</div>
